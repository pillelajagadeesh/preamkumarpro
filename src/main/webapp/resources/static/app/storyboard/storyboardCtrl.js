appRaamApp.controller('StoryboardController', ['$scope', '$rootScope','$timeout', '$location', 'StoryboardService', '$window',
function ($scope, $rootScope, $timeout,  $location, StoryboardService, $window) {
// while refreshing the page or loading of page we are remove the cookie store loan details.
}
]);
